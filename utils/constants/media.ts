export enum MediaTypes {
    IMAGE = 'image',
    VIDEO = 'video',
    PDF_DOCUMENT = 'pdf_document',
    SVG_DOCUMENT = 'svg_document',
    DOCUMENT = 'document',
    AUDIO = 'audio',
    GIF = 'gif',
}

export enum MediaUploadStatus {
    LOADING = 'loading',
    UPLOADING = 'uploading',
    UPLOADED = 'uploaded',
    FAILED = 'failed',
}

export enum ChannelMediaSource {
    HEADER_MEDIA = 'headerMedia',
    GALLERY_MEDIA = 'galleryMedia',
}
